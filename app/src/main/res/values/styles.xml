<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/windowbackground</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.Center" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_icon</item>
    </style>

    <style name="CustomCheckboxTheme" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_select</item>
    </style>>
</resources>
